// Minimal wrapper: load canonical script.clean.js as the only active script
import './script.clean.js';

// Keep the wrapper minimal to avoid duplicate execution and bugs; edit `script.clean.js` for runtime behavior.
